/**

 * Plugins: volume-only — widgets and manifests under `/app/plugins/custom`.

 * Install via Plugin Store (GitHub `plugins-pack/`) or ZIP upload.

 */



export function isVolumeOnlyPlugins(): boolean {

  return true

}



/** @deprecated Volume-only deployment; always false. */

export function isBuiltinPluginsEnabled(): boolean {

  return false

}


